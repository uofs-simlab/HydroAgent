# Symfluence Assistant — Python dependencies
# Install in a dedicated venv (recommended). Geospatial stack may be easier via conda; see README.

streamlit>=1.40,<2
streamlit-folium>=0.18
folium>=0.15
geopandas>=0.14
matplotlib>=3.8
pandas>=2.0
PyYAML>=6.0
openai>=1.40
google-genai>=1.0
anthropic>=0.40

# Optional helpers (Results tab: mizuRoute CSV tools)
netCDF4>=1.6

# LangPrompt integration (prompt library / embeddings)
langchain-core>=0.3.0
langchain-openai>=0.2.0
numpy>=1.24